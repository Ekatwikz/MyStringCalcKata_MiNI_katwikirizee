namespace StringCalcLib;
public class StringCalc
{
    public int Add(string input)
    {
        if (String.IsNullOrEmpty(input))
        {
            return 0;
        }

        throw new NotImplementedException("Not quite done...");
    }
}
